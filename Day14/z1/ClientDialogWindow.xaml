<Window x:Class="CRMBusiness.ClientDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Клиент" 
        Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Информация о клиенте" 
                   FontSize="18" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <TextBlock Text="ФИО:" Grid.Row="1" FontWeight="Bold" Margin="0,5,0,5"/>
        <TextBox x:Name="FullNameTextBox" Grid.Row="2" Margin="0,0,0,10" Height="30" FontSize="14"/>

        <TextBlock Text="Телефон:" Grid.Row="3" FontWeight="Bold" Margin="0,5,0,5"/>
        <TextBox x:Name="PhoneTextBox" Grid.Row="4" Margin="0,0,0,10" Height="30" FontSize="14"/>

        <TextBlock Text="Email:" Grid.Row="5" FontWeight="Bold" Margin="0,5,0,5"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="6" Margin="0,0,0,10" Height="30" FontSize="14"/>

        <TextBlock Text="История взаимодействий:" Grid.Row="7" FontWeight="Bold" Margin="0,5,0,5"/>
        <TextBox x:Name="HistoryTextBox" Grid.Row="8" 
                 Height="100" 
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,20"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="OkButton" 
                    Content="OK" 
                    Click="OkButton_Click"
                    Width="80" 
                    Height="35"
                    Margin="10"
                    Background="#4CAF50"
                    Foreground="White"
                    FontWeight="Bold"/>
            <Button x:Name="CancelButton" 
                    Content="Отмена" 
                    Click="CancelButton_Click"
                    Width="80" 
                    Height="35"
                    Margin="10"
                    Background="#9E9E9E"
                    Foreground="White"
                    FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>