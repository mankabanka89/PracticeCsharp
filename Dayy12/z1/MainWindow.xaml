<Window x:Class="CRMBusiness.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CRM - Система управления клиентами" 
        Height="550" Width="900"
        WindowStartupLocation="CenterScreen"
        MinWidth="800">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CRM - Управление клиентами" 
                   FontSize="24" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <!-- ЛЕВАЯ ЧАСТЬ: Список клиентов -->
            <Grid Grid.Column="0" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Список клиентов" 
                           FontSize="16" 
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <ListView x:Name="ClientsListView" 
                          Grid.Row="1"
                          SelectionChanged="ClientsListView_SelectionChanged"
                          FocusVisualStyle="{x:Null}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                            <GridViewColumn Header="ФИО" Width="180" DisplayMemberBinding="{Binding FullName}"/>
                            <GridViewColumn Header="Телефон" Width="120" DisplayMemberBinding="{Binding Phone}"/>
                            <GridViewColumn Header="Email" Width="150" DisplayMemberBinding="{Binding Email}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>

            <!-- ПРАВАЯ ЧАСТЬ: Форма для редактирования -->
            <Border Grid.Column="1" 
                    Margin="10" 
                    Background="#F0F0F0" 
                    CornerRadius="8"
                    Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Данные клиента" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Margin="0,0,0,15"
                               HorizontalAlignment="Center"/>

                    <TextBlock Text="ФИО:" Grid.Row="1" FontWeight="Bold" Margin="5,5,5,0"/>
                    <TextBox x:Name="FullNameTextBox" Grid.Row="2" Margin="5"/>

                    <TextBlock Text="Телефон:" Grid.Row="3" FontWeight="Bold" Margin="5,5,5,0"/>
                    <TextBox x:Name="PhoneTextBox" Grid.Row="4" Margin="5"/>

                    <TextBlock Text="Email:" Grid.Row="5" FontWeight="Bold" Margin="5,5,5,0"/>
                    <TextBox x:Name="EmailTextBox" Grid.Row="6" Margin="5"/>

                    <TextBlock Text="История взаимодействий:" Grid.Row="7" FontWeight="Bold" Margin="5,5,5,0"/>
                    <TextBox x:Name="HistoryTextBox" Grid.Row="8" 
                             Height="80" 
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="5"/>

                    <!-- Кнопки - теперь точно видны -->
                    <WrapPanel Grid.Row="9" 
                               HorizontalAlignment="Center"
                               Margin="0,20,0,10">
                        <Button x:Name="AddButton" 
                                Content="Добавить" 
                                Click="AddButton_Click"
                                Background="#4CAF50"
                                Foreground="White"
                                Width="110"
                                Height="35"
                                Margin="5"/>

                        <Button x:Name="EditButton" 
                                Content="Редактировать" 
                                Click="EditButton_Click"
                                Background="#2196F3"
                                Foreground="White"
                                Width="110"
                                Height="35"
                                Margin="5"/>

                        <Button x:Name="DeleteButton" 
                                Content="Удалить" 
                                Click="DeleteButton_Click"
                                Background="#F44336"
                                Foreground="White"
                                Width="110"
                                Height="35"
                                Margin="5"/>

                        <Button x:Name="ClearButton" 
                                Content="Очистить" 
                                Click="ClearButton_Click"
                                Background="#9E9E9E"
                                Foreground="White"
                                Width="110"
                                Height="35"
                                Margin="5"/>
                    </WrapPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>